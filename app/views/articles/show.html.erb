<p id="notice"><%= notice %></p>

<h1><%= @article.title %></h1>
<h3> <%= @article.subtitle %> </h3>

<p>
  <%= raw @article.content %>
</p>

<!-- CRUD options for author -->
<% if user_signed_in? && current_user.id == @article.user_id%>
  <%= link_to 'Edit', edit_article_path(@article) %> |
  <%= link_to 'Back', articles_path %>
<% end %>
